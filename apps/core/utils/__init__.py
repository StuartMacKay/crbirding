"""Standalone helper modules for the core app -- formatting, matching,
bulk import, and small per-request-state helpers (alphabets, htmx).

Each is its own thing; import from the specific submodule
(e.g. `from core.utils.formatting import describe_journey`), not from
this package directly.
"""
